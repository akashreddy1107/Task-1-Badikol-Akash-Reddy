# Task 1: CLI To-Do List Application

## Description
This is a simple Command Line Interface (CLI) To-Do List application written in Python. It allows users to manage their daily tasks directly from the terminal. 

## Features
- **Add Task**: Users can input a task description, which is then added to a running list of tasks.
- **View Tasks**: Displays all the added tasks in a neat bulleted list format.
- **Exit**: Allows the user to gracefully close the program.
- **Input Handling**: Handles invalid menu selections by alerting the user.

## How to Run the Code
1. Open your terminal or command prompt.
2. Navigate to the folder containing `week1.py`.
3. Run the script using Python:
   ```bash
   python week1.py
   ```

## Output Screenshot
*(Add your output screenshot here!)*
<!-- Place your screenshot image in the repository and link it below -->
![Task 1 Output Screenshot](week1_output.png)

---

### How to upload your output screenshot:
1. Save your screenshot as `week1_output.png`.
2. Upload it to this repository root on GitHub (using the **Add file** -> **Upload files** button on the GitHub page, or commit it via Git).
3. The screenshot will automatically show up in this README.
